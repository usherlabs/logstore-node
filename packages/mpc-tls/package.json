{
	"name": "@logsn/mpc-tls",
	"version": "1.0.0",
	"description": "Notarize requests by passing them through a proxy which forms an mpc tls connection to notarize data.",
	"scripts": {
		"build": "cargo build --release",
		"postbuild": "cd scripts && sh ./move_assets.sh"
	},
	"keywords": [],
	"author": "",
	"license": "ISC"
}
